<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/output.css">
    <title>Home | moment</title>
</head>
<body>
    <header>
        <nav class="lg:h-20 lg:w-screen lg:flex lg: items-center lg:justify-between bg-zinc-900 text-orange-300 fixed z-10">
            <p class="ml-24">moment</p>
            <ul class="lg:flex lg:justify-around lg:items-center lg:w-1/3 lg:h-20 lg:mr-16">
                <a href="/">Home</a>
                <a href="/festivals">All Festivals</a>
                <a href="/genres">All Genres</a>
                <a href="/festform">Add Festival</a>
            </ul>
        </nav>
        <section class="lg:w-full lg:h-[47rem] lg:bg-hero lg:flex lg:justify-center lg:items-center">
            <h1 class="lg:text-7xl text-slate-50">moment</h1>
        </section>
    </header>
    
    <h2 class="lg:text-7xl bg-gradient-to-r from-pink-500 to-yellow-500 bg-clip-text text-transparent text-center pt-20">Our curated top list</h2>
    <main class="lg:flex lg:flex-wrap lg:px-32 lg:pb-20">
        <% festivals.forEach((festival) => { %>

            <figure class="lg:w-64 lg:h-80 lg:mx-4 lg:my-10 ">
                <a href="<%=festival.id %>">
                    <img class="lg:w-64 lg:h-80 lg:rounded-lg hover:transition ease-in hover:-translate-y-1 hover:scale-110 duration-300" src="<%= festival.image_url %>" alt="">
                </a>
               <div class="py-2">
                   <p><%= festival.name %></p>
                   <p class="text-zinc-500"><%= festival.location  %></p>
               </div>
            </figure>
       <% }) %>
    </main>
   
</body>
</html>