<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/output.css">
    <title>Festival | moment</title>
</head>
<body>
    <nav
        class="lg:h-20 lg:w-screen lg:flex lg: items-center lg:justify-between bg-zinc-900 text-orange-300 fixed z-10"
      >
        <p class="ml-24">moment</p>
        <ul class="lg:flex lg:justify-around lg:items-center lg:w-1/3 lg:h-20 lg:mr-16">
          <a href="/">Home</a>
          <a href="/festivals">All Festivals</a>
          <a href="/genres">All Genres</a>
          <a href="/festform">Add Festival</a>
        </ul>
      </nav>
    <main class="lg:flex lg:px-20 lg:py-20">
        <section class="w-1/2">
            <% festival.forEach((fest) => { %>
                <div class="lg:w-5/6 lg:h-screen">
                    <img class="lg:object-fill lg:w-full lg:h-screen" src="<%= fest.image_url %>" alt="festival lineup">
                </div>
             <% }) %>
            
        </section>
        <aside class="lg:w-1/2">
            <% festival.forEach((fest) => { %>
                <h2 class="lg:text-6xl lg:mt-10"><%= fest.name  %></h2>
                <p class="lg:text-xl lg:mt-4"><%= fest.location %></p>
                <p><span class="font-bold">Date:</span> <%=fest.start_date%></p>
            <% }) %>
        </aside>
    </main>
</body>
</html>