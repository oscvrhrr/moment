<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/output.css">
    <title>Details</title>
</head>
<body class="bg-zinc-900">
    <nav
        class="lg:h-16 lg:w-screen lg:flex lg: items-center lg:justify-between text-zinc-100 z-10 border-b border-zinc-800"
      >
      <div class="flex items-center ml-32">
        <img class="h-12" src="/speaker-music-and-multimedia-svgrepo-com.svg" alt="">
        <p>festival inventory</p>
    </div>
        <ul class="lg:flex lg:justify-around lg:items-center lg:w-1/3 lg:h-20 lg:mr-16">
          <a class="hover:bg-zinc-700 hover:transition ease-in p-1 rounded" href="/">Home</a>
          <a class="hover:bg-zinc-700 hover:transition ease-in p-1 rounded" href="/festivals">All Festivals</a>
          <a class="hover:bg-zinc-700 hover:transition ease-in p-1 rounded" href="/genres">All Genres</a>
          <a class="hover:bg-zinc-700 hover:transition ease-in p-1 rounded" href="/add-fest">Add Festival</a>
        </ul>
      </nav>
    <main class="lg:flex lg:px-20 lg:py-6">
        <section class="w-1/2">
            <% festival.forEach((fest) => { %>
                <div class="lg:w-9/12 lg:h-[45rem]">
                    <img class="lg:object-fill lg:w-full lg:h-[45rem]" src="<%= fest.image_url %>" alt="festival lineup">
                </div>
             <% }) %>
            
        </section>
        <aside class="lg:w-1/2 text-gray-100">
            <% festival.forEach((fest) => { %>
                <h2 class="lg:text-5xl font-medium lg:mt-10"><%= fest.name  %></h2>
                <p class="lg:text-xl lg:mt-4"><%= fest.location %></p>
                <p><span class="font-bold">Date:</span> <%=fest.start_date%></p>
                <div class="mt-8 flex">
                    <a href="/festivals/<%= fest.id %>/updateItem" class="mr-2 bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 px-4 rounded shadow-lg transition-all duration-300 ease-in-out">Edit</a>
                    <form method="post" action="/festivals/<%= fest.id %>/delete">
                        <button type="submit" class="bg-red-600 hover:bg-red-700 text-white font-medium py-2 px-4 rounded shadow-lg transition-all duration-300 ease-in-out">Delete</button>
                    </form>
                </div>
            <% }) %>
        </aside>
    </main>
</body>
</html>